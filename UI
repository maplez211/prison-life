-- UI Script Precision | Prison Life - Obsidian Library Version
local repo = "https://raw.githubusercontent.com/deividcomsono/Obsidian/main/"
local Library = loadstring(game:HttpGet(repo .. "Library.lua"))()
local ThemeManager = loadstring(game:HttpGet(repo .. "addons/ThemeManager.lua"))()
local SaveManager = loadstring(game:HttpGet(repo .. "addons/SaveManager.lua"))()

local Options = Library.Options
local Toggles = Library.Toggles

local Players = game:GetService("Players")
local player = Players.LocalPlayer
local RunService = game:GetService("RunService")

-- ============ WINDOW SETUP ============
local Window = Library:CreateWindow({
    Title = "Precision Hub | Prison Life",
    Footer = "Version 0.1.9 | by 2knw",
    Icon = 83140129251904,
    NotifySide = "Right",
    ShowCustomCursor = true,
})

-- ============ TABS ============
local Tabs = {
    Main = Window:AddTab("Main", "settings"),
    Aimbot = Window:AddTab("Aimbot", "crosshair"),
    Players = Window:AddTab("Players", "user"),
    Visuals = Window:AddTab("Visuals", "eye"),
    Teleports = Window:AddTab("Teleports", "map-pin"),
    ["UI Settings"] = Window:AddTab("UI Settings", "settings"),
}

Window:SelectTab(Tabs.Main)

-- ============ MAIN TAB ============
local MainGroupBox = Tabs.Main:AddGroupbox({
    Name = "Player",
    Side = "Left",
})

MainGroupBox:AddSlider("SpinbotSpeed", {
    Text = "Spinbot",
    Tooltip = "Spins your character at the chosen speed.",
    Default = 0,
    Min = 0,
    Max = 250,
    Rounding = 1,
    Callback = function(Value)
        getgenv().SilentAimSettings.SpinbotSpeed = Value
    end,
})

-- ============ PLAYERS TAB ============
local PlayersLeftBox = Tabs.Players:AddGroupbox({
    Name = "Teleport",
    Side = "Left",
})

local playerOptions = {"Select a player..."}
for _, plr in ipairs(Players:GetPlayers()) do
    if plr ~= player then
        table.insert(playerOptions, plr.Name)
    end
end

PlayersLeftBox:AddDropdown("TeleportPlayer", {
    Values = playerOptions,
    Default = 1,
    Text = "Teleport to Player",
    Tooltip = "Select a player to teleport to them",
    Callback = function(Value)
        if not Value or Value == "Select a player..." then return end
        local target = Players:FindFirstChild(Value)
        if target and target.Character then
            local hrp = target.Character:FindFirstChild("HumanoidRootPart")
            if hrp then
                local c = player.Character
                if c and c:FindFirstChild("HumanoidRootPart") then
                    c.HumanoidRootPart.CFrame = CFrame.new(hrp.Position + Vector3.new(0, 3, 0))
                end
            end
        end
    end,
})

PlayersLeftBox:AddButton({
    Text = "Update Players List",
    Func = function()
        local options = {"Select a player..."}
        for _, plr in ipairs(Players:GetPlayers()) do
            if plr ~= player then
                table.insert(options, plr.Name)
            end
        end
        Options.TeleportPlayer:SetValues(options)
    end,
})

-- ============ VIEW PLAYER SECTION ============
local PlayersRightBox = Tabs.Players:AddGroupbox({
    Name = "View Player",
    Side = "Right",
})

local viewPlayerOptions = {"Select a player..."}
for _, plr in ipairs(Players:GetPlayers()) do
    if plr ~= player then
        table.insert(viewPlayerOptions, plr.Name)
    end
end

local SPECTATE_PLAYER = nil
local spectateConnection = nil

local function stopSpectate()
    if spectateConnection then
        spectateConnection:Disconnect()
        spectateConnection = nil
    end
    if player.Character then
        local humanoid = player.Character:FindFirstChildOfClass("Humanoid")
        if humanoid then
            workspace.CurrentCamera.CameraSubject = humanoid
        end
    end
end

local function startSpectate(targetName)
    if spectateConnection then
        spectateConnection:Disconnect()  
        spectateConnection = nil
    end
    
    local target = Players:FindFirstChild(targetName)
    if not target or not target.Character then return end
    
    local humanoid = target.Character:FindFirstChildOfClass("Humanoid")
    if not humanoid then return end
    
    workspace.CurrentCamera.CameraSubject = humanoid
    spectateConnection = RunService.RenderStepped:Connect(function()
        if target and target.Character then
            local hum = target.Character:FindFirstChildOfClass("Humanoid")
            if hum and hum.Health > 0 then
                workspace.CurrentCamera.CameraSubject = hum
            end
        end
    end)
end

PlayersRightBox:AddDropdown("ViewPlayer", {
    Values = viewPlayerOptions,
    Default = 1,
    Text = "View Player",
    Tooltip = "Select a player to spectate",
    Callback = function(Value)
        if not Value or Value == "Select a player..." then
            SPECTATE_PLAYER = nil
            stopSpectate()
            return
        end
        SPECTATE_PLAYER = Value
        startSpectate(Value)  
    end,
})

PlayersRightBox:AddButton({
    Text = "Update Players List",
    Func = function()
        local options = {"Select a player..."}
        for _, plr in ipairs(Players:GetPlayers()) do
            if plr ~= player then
                table.insert(options, plr.Name)
            end
        end
        SPECTATE_PLAYER = nil
        stopSpectate()
        Options.ViewPlayer:SetValues(options)
    end,
})

-- ============ AIMBOT TAB ============
local AimbotLeftBox = Tabs.Aimbot:AddGroupbox({
    Name = "Aim Settings",
    Side = "Left",
})

AimbotLeftBox:AddToggle("SilentAim", {
    Text = "Silent Aim",
    Tooltip = "Silent Aim method by 7zz",
    Default = true,
    Callback = function(v)
        getgenv().SilentAimSettings.Enabled = v
    end,
})

AimbotLeftBox:AddToggle("WallCheck", {
    Text = "Wall Check",
    Tooltip = "Ignore Players behind Walls",
    Default = true,
    Callback = function(v)
        getgenv().SilentAimSettings.VisibleCheck = v
    end,
})

AimbotLeftBox:AddToggle("TeamCheck", {
    Text = "Team Check",
    Tooltip = "Ignore Players on your Team",
    Default = true,
    Callback = function(v)
        getgenv().SilentAimSettings.TeamCheck = v
    end,
})

AimbotLeftBox:AddToggle("FriendCheck", {
    Text = "Friend Check",
    Tooltip = "Ignore your Friends",
    Default = true,
    Callback = function(v)
        getgenv().SilentAimSettings.FriendCheck = v
    end,
})

AimbotLeftBox:AddToggle("IgnoreInmates", {
    Text = "Ignore Inmates",
    Tooltip = "Ignore Inmate Players",
    Default = false,
    Callback = function(v)
        getgenv().SilentAimSettings.IgnoreInmates = v
    end,
})

AimbotLeftBox:AddToggle("IgnoreGuards", {
    Text = "Ignore Guards",
    Tooltip = "Ignore Guard Players",
    Default = false,
    Callback = function(v)
        getgenv().SilentAimSettings.IgnoreGuards = v
    end,
})

AimbotLeftBox:AddToggle("IgnoreCriminals", {
    Text = "Ignore Criminals",
    Tooltip = "Ignore Criminal Players",
    Default = false,
    Callback = function(v)
        getgenv().SilentAimSettings.IgnoreCriminals = v
    end,
})

AimbotLeftBox:AddToggle("HostileCheck", {
    Text = "Hostile Check",
    Tooltip = "Only target hostile inmates",
    Default = false,
    Callback = function(v)
        getgenv().SilentAimSettings.HostileCheck = v
    end,
})

AimbotLeftBox:AddToggle("TrespassCheck", {
    Text = "Trespass Check",
    Tooltip = "Only target trespassing inmates",
    Default = false,
    Callback = function(v)
        getgenv().SilentAimSettings.TrespassCheck = v
    end,
})

-- ============ AIMBOT SLIDERS ============
local AimbotMidBox = Tabs.Aimbot:AddGroupbox({
    Name = "Smoothing & Accuracy",
    Side = "Left",
})

AimbotMidBox:AddSlider("Smoothing", {
    Text = "Smoothing",
    Default = 50,
    Min = 0,
    Max = 100,
    Rounding = 1,
    Callback = function(Value)
        getgenv().SilentAimSettings.Smoothing = Value / 100
    end,
})

AimbotMidBox:AddSlider("HitChance", {
    Text = "Hit Chance",
    Default = 100,
    Min = 0,
    Max = 100,
    Rounding = 1,
    Callback = function(Value)
        getgenv().SilentAimSettings.HitChance = Value
    end,
})

-- ============ TARGET SELECTION ============
local TargetBox = Tabs.Aimbot:AddGroupbox({
    Name = "Target Selection",
    Side = "Right",
})

local targetOptions = {}
for _, plr in ipairs(Players:GetPlayers()) do
    if plr ~= player then
        table.insert(targetOptions, plr.Name)
    end
end

TargetBox:AddDropdown("TargetOn", {
    Values = targetOptions,
    Default = 1,
    Text = "Target On",
    Tooltip = "Select specific players to target",
    Multi = true,
    Callback = function(Value)
        if not Value or #Value == 0 then
            getgenv().SilentAimSettings.TargetOnPlayer = {}
        else
            getgenv().SilentAimSettings.TargetOnPlayer = Value
        end
    end,
})

TargetBox:AddButton({
    Text = "Reset Target",
    Func = function()
        getgenv().SilentAimSettings.TargetOnPlayer = {}
        local newOptions = {}
        for _, plr in ipairs(Players:GetPlayers()) do
            if plr ~= player then
                table.insert(newOptions, plr.Name)
            end
        end
        Options.TargetOn:SetValues(newOptions)
    end,
})

TargetBox:AddDropdown("IgnoreTarget", {
    Values = targetOptions,
    Default = 1,
    Text = "Ignore Target",
    Tooltip = "Select players to completely ignore",
    Multi = true,
    Callback = function(Value)
        getgenv().SilentAimSettings.IgnoreTargets = Value or {}
    end,
})

TargetBox:AddButton({
    Text = "Reset Ignore List",
    Func = function()
        getgenv().SilentAimSettings.IgnoreTargets = {}
        local newOptions = {}
        for _, plr in ipairs(Players:GetPlayers()) do
            if plr ~= player then
                table.insert(newOptions, plr.Name)
            end
        end
        Options.IgnoreTarget:SetValues(newOptions)
    end,
})

TargetBox:AddDropdown("TargetPart", {
    Values = {"Head", "Torso", "Left Arm", "Right Arm", "Left Leg", "Right Leg", "HumanoidRootPart"},
    Default = "HumanoidRootPart",
    Text = "Target Part",
    Tooltip = "Choose which body part the Silent Aim will lock onto",
    Callback = function(Value)
        getgenv().SilentAimSettings.TargetPart = Value
    end,
})

-- ============ FOV SECTION ============
local FOVBox = Tabs.Aimbot:AddGroupbox({
    Name = "FOV",
    Side = "Right",
})

FOVBox:AddSlider("FOVSize", {
    Text = "FOV Size",
    Default = 100,
    Min = 30,
    Max = 500,
    Rounding = 10,
    Callback = function(Value)
        getgenv().SilentAimSettings.FOVRadius = Value
    end,
})

FOVBox:AddDropdown("FOVColor", {
    Values = {"Black", "Red", "Green", "Blue", "Yellow", "Purple", "Cyan", "Orange", "Pink", "White", "Rainbow"},
    Default = "White",
    Text = "FOV Color",
    Tooltip = "Choose the FOV circle color",
    Callback = function(Value)
        setFOVColor(Value)
    end,
})

FOVBox:AddToggle("HideFOV", {
    Text = "Hide FOV Circle",
    Default = false,
    Callback = function(v)
        toggleFOVCircle(v)
    end,
})

-- ============ VISUALS TAB ============
local VisualsLeftBox = Tabs.Visuals:AddGroupbox({
    Name = "Visual Options",
    Side = "Left",
})

VisualsLeftBox:AddToggle("ESP", {
    Text = "ESP",
    Default = false,
    Callback = function(v)
        getgenv().SilentAimSettings.ESPEnabled = v
        updateESP()
    end,
})

VisualsLeftBox:AddToggle("TracerESP", {
    Text = "Tracer ESP",
    Tooltip = "Shows the beam tracer to your target",
    Default = false,
    Callback = function(v)
        getgenv().SilentAimSettings.TracerESPEnabled = v
    end,
})

VisualsLeftBox:AddToggle("HideESPNames", {
    Text = "Hide ESP Names",
    Tooltip = "Hides the names above players when ESP is active",
    Default = false,
    Callback = function(v)
        getgenv().SilentAimSettings.ESPNamesEnabled = not v
        updateESP()
    end,
})

-- ============ CUSTOMIZATION ============
local CustomBox = Tabs.Visuals:AddGroupbox({
    Name = "Customization",
    Side = "Right",
})

CustomBox:AddToggle("RemoveFog", {
    Text = "Remove Fog",
    Tooltip = "Removes the fog from the game",
    Default = false,
    Callback = function(v)
        if v then
            game:GetService("Lighting").FogEnd = 100000
            game:GetService("Lighting").FogStart = 100000
        else
            game:GetService("Lighting").FogEnd = 5000
            game:GetService("Lighting").FogStart = 500
        end
    end,
})

CustomBox:AddToggle("ToggleRain", {
    Text = "Toggle Rain",
    Tooltip = "Makes it rain with sound effect",
    Default = false,
    Callback = function(v)
        toggleRain(v)
    end,
})

CustomBox:AddDropdown("TracerColor", {
    Values = {"Black", "Red", "Green", "Blue", "Yellow", "Purple", "Cyan", "Orange", "Pink", "White"},
    Default = "Pink",
    Text = "Tracer Color",
    Tooltip = "Choose the Tracer beam color",
    Callback = function(Value)
        setTracerColor(Value)
    end,
})

CustomBox:AddDropdown("FogColor", {
    Values = {"Default", "Black", "Red", "Green", "Blue", "Yellow", "Purple", "Cyan", "Orange", "Pink", "White"},
    Default = "Default",
    Text = "Fog Color",
    Tooltip = "Choose the fog color",
    Callback = function(Value)
        setFogColor(Value)
    end,
})

CustomBox:AddDropdown("Skyboxes", {
    Values = {"None", "Gray Skybox", "2knw & Donk666 Skybox", "Snow Skybox", "Cartoon Skybox", "Lince Skybox", "Red Skybox", "Slam Skybox", "Green Eyeball Skybox", "Morioh Skybox", "Rainy Skybox"},
    Default = "None",
    Text = "Skyboxes",
    Tooltip = "Choose your skybox",
    Callback = function(Value)
        setSkybox(Value)
    end,
})

-- ============ TELEPORTS TAB ============
local TeleportsBox = Tabs.Teleports:AddGroupbox({
    Name = "Teleports",
    Side = "Left",
})

local teleports = {
    {"Nexus 1", Vector3.new(864, 100, 2406)},
    {"Nexus 2", Vector3.new(969, 100, 2289)},
    {"Cells", Vector3.new(889, 100, 2500)},
    {"Cafeteria", Vector3.new(964, 103, 2286)},
    {"Kitchen", Vector3.new(965, 100, 2240)},
    {"Yard", Vector3.new(801, 98, 2499)},
    {"Armory", Vector3.new(803, 108, 2257)},
    {"Break Room", Vector3.new(794, 101, 2246)},
    {"Entrance", Vector3.new(737, 100, 2287)},
    {"Front Wall 1", Vector3.new(510, 122, 2327)},
    {"Front Wall 2", Vector3.new(505, 122, 2143)},
    {"Guard Tower 1", Vector3.new(825, 126, 2072)},
    {"Guard Tower 2", Vector3.new(816, 126, 2582)},
    {"Sewage", Vector3.new(917, 79, 2102)},
    {"Escape the Prison", Vector3.new(840, 98, 2598)},
}

for _, tp in ipairs(teleports) do
    TeleportsBox:AddButton({
        Text = tp[1],
        Func = function()
            local c = player.Character
            if c and c:FindFirstChild("HumanoidRootPart") then
                c.HumanoidRootPart.CFrame = CFrame.new(tp[2])
            end
        end,
    })
end

-- ============ UI SETTINGS ============
local MenuGroup = Tabs["UI Settings"]:AddGroupbox({
    Name = "Menu",
    Side = "Left",
})

MenuGroup:AddToggle("KeybindMenuOpen", {
    Text = "Open Keybind Menu",
    Default = Library.KeybindFrame.Visible,
    Callback = function(value)
        Library.KeybindFrame.Visible = value
    end,
})

MenuGroup:AddToggle("ShowCustomCursor", {
    Text = "Custom Cursor",
    Default = Library.ShowCustomCursor,
    Callback = function(Value)
        Library.ShowCustomCursor = Value
    end,
})

MenuGroup:AddToggle("AlwaysOnTop", {
    Text = "Always On Top",
    Default = Window.AlwaysOnTop,
    Callback = function(Value)
        Window:SetAlwaysOnTop(Value)
    end,
})

MenuGroup:AddDropdown("NotificationSide", {
    Values = {"Left", "Right"},
    Default = "Right",
    Text = "Notification Side",
    Callback = function(Value)
        Library:SetNotifySide(Value)
    end,
})

MenuGroup:AddSlider("UICornerSlider", {
    Text = "Corner Radius",
    Default = Library.CornerRadius,
    Min = 0,
    Max = 20,
    Rounding = 0,
    Callback = function(value)
        Window:SetCornerRadius(value)
    end,
})

MenuGroup:AddDivider()

MenuGroup:AddLabel("Menu Bind"):AddKeyPicker("MenuKeybind", {
    Default = "RightShift",
    NoUI = true,
    Text = "Menu keybind",
})

MenuGroup:AddButton("Unload", function()
    Library:Unload()
end)

Library.ToggleKeybind = Options.MenuKeybind

-- ============ ADDON SETUP ============
ThemeManager:SetLibrary(Library)
SaveManager:SetLibrary(Library)

SaveManager:IgnoreThemeSettings()
SaveManager:SetIgnoreIndexes({ "MenuKeybind" })

ThemeManager:SetFolder("PrecisionHub")
SaveManager:SetFolder("PrecisionHub/PrisonLife")

SaveManager:BuildConfigSection(Tabs["UI Settings"])
ThemeManager:ApplyToTab(Tabs["UI Settings"])

SaveManager:LoadAutoloadConfig()
